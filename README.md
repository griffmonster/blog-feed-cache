# blog-feed-cache
This script recurses through the multiple feeds of a blogger site to generate a json data file of all posts. This can then be used as a cache input for a blogger page that can generate lists of posts based upon the feed.

